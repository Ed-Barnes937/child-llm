services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: childsafe
      POSTGRES_PASSWORD: childsafe
      POSTGRES_DB: childsafe
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
